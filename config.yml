model:
  qubit: # Absolute paths of optimization models
    fQ:
      pad_gap: /Users/jagan/Codes/mqhad/models/polynomial_ridge_regression_fQ_pad_gap_in_um.pkl
      pad_height: /Users/jagan/Codes/mqhad/models/polynomial_ridge_regression_fQ_pad_height_in_um.pkl
  resonator: # Not used at the moment

target:
  qubit:
    specific: 
    # Qubit-specific parameter configuration, i.e, 
    # Q_0: 
    #  fQ: 5.3   
    # Qubit frequencies configured here will take precendence over
    # qubit frequncies generated by architecture generator
    general: 
    # Qubit parameters shared by all qubits
      EC/EJ: 50
  resonator: # Not used at the moment
    specific:
    general:
  